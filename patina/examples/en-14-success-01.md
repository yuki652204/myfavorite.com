---
pattern: 14
type: success
name: Boldface Overuse
pack: en-style
language: en
---

# Pattern 14 (en): Boldface Overuse — Success Case

## Input Text

> Over the past decade, **machine learning** has emerged as one of the most influential branches of **artificial intelligence**. At its core, **neural networks** loosely mimic the structure of biological neurons, enabling models to detect complex patterns in raw data. A particularly powerful variant, **deep learning**, stacks many hidden layers to learn hierarchical representations. Most introductory courses begin with **supervised learning**, where labeled examples guide the training process, before moving on to techniques like **gradient descent**, which iteratively adjusts model weights to minimize prediction error. Together, these ideas form the foundation of modern AI research.

## Expected Output

> Over the past decade, machine learning has emerged as one of the most influential branches of artificial intelligence. At its core, neural networks loosely mimic the structure of biological neurons, enabling models to detect complex patterns in raw data. A particularly powerful variant, deep learning, stacks many hidden layers to learn hierarchical representations. Most introductory courses begin with supervised learning, where labeled examples guide the training process, before moving on to techniques like gradient descent, which iteratively adjusts model weights to minimize prediction error. Together, these ideas form the foundation of modern AI research.

## Applied Pattern

- Pattern 14 (Boldface Overuse): Six bolded terms in a single paragraph — "machine learning", "artificial intelligence", "neural networks", "deep learning", "supervised learning", "gradient descent" — used as inline emphasis in flowing prose where no term requires visual distinction.

## Judgment

**Success** — The fire condition is clearly met: six bolded terms in a single paragraph of expository prose, far exceeding the 3-per-paragraph threshold. None of the bolded terms serve a navigational or safety purpose; they read like a textbook study guide highlighting vocabulary words. Removing all bold produces clean prose that still communicates every concept without visual clutter.
