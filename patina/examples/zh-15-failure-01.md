---
pattern: 15
type: failure
name: 内联标题列表
pack: zh-style
language: zh
---

# Pattern 15 (zh): 内联标题列表 — 失败案例（误报）

## 输入文本

> - **timeout:** 请求被中止前的最长等待时间，默认 `5000`。
> - **retries:** 失败后的重试次数，默认 `3`。
> - **baseURL:** 所有相对路径的根地址。

## 期望输出

> （不修改 — Pattern 15 不应触发这段文本）

## 适用模式

- Pattern 15 (内联标题列表): 多个条目采用“**标签:** 说明”格式。

## 判定

**失败（误报）** — 这是API参数说明，标签-说明结构是参考文档的标准格式。改成普通散文反而不利于查找。
